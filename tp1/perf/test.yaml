config:
  environments:
    gunicorn:
      target: 'http://localhost:5555/gunicorn/'
      plugins:
        statsd:
          host: localhost
          port: 8125
          prefix: "artillery-gunicorn"
    node:
      target: 'http://localhost:5555/node/'
      plugins:
        statsd:
          host: localhost
          port: 8125
          prefix: "artillery-node"
    n-gunicorn:
      target: 'http://localhost:5555/gunicorn_replicated/'
      plugins:
        statsd:
          host: localhost
          port: 8125
          prefix: "artillery-n-gunicorn"
    w-gunicorn:
      target: 'http://localhost:5555/worker-gunicorn/'
      plugins:
        statsd:
          host: localhost
          port: 8125
          prefix: "artillery-w-gunicorn"
    cluster-node:
      target: 'http://localhost:5555/cluster-node/'
      plugins:
        statsd:
          host: localhost
          port: 8125
          prefix: "artillery-cluster-node"

  pool: 50 # All HTTP requests from all virtual users will be sent over the same connections

  phases:
    # Crear "arrivalRate" usuarios por segundo, durante un intervalo de "duration" segundos
    - name: Constant
      duration: 60
      arrivalRate: 1000

scenarios:
  - name: Root (/)
    flow:
      - get:
          url: '/'
